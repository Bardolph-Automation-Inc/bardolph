server.modules += ( "mod_fastcgi" )

fastcgi.server = ("/lights" =>
    ((
        "socket" => "/dev/shm/lights.sock",
        "check-local" => "disable",
        "max-procs" => 1
    ))
)

url.rewrite-once = (
    "^(/.*)$" => "/lights$1"
)
